# DASM Investor Portal — متغيرات البيئة
# انسخ هذا الملف إلى .env.local وعدّل القيم

# ── API داسم الرئيسي ──────────────────────────────────────────
# محلي: http://localhost:8000/api  |  إنتاج: https://api.dasm.com.sa/api
VITE_DASM_API_URL=http://localhost:8000/api

# ── الدومين الرئيسي لداسم (للتوجيه لصفحة SSO) ───────────────
# محلي: http://localhost:3000  |  إنتاج: https://www.dasm.com.sa
VITE_DASM_SSO_URL=http://localhost:3000
